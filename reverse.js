
// Función para dividir la cadena en palabras
export function dividirEnPalabras(cadena) {
    return cadena.split(' ');
}

// Función para invertir un array
export function invertirArray(array) {
    return array.reverse();
}